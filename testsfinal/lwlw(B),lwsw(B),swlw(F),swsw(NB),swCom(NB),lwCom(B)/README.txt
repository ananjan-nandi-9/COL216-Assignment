lwlw(B) : lw then lw on same reg is blocking
lwsw(B) : lw then sw on same command is blocking
swlw(F) : sw then lw on same command is forwarded
swsw(NB) : sw then sw on same command is non-blocking
lwcom(B) : lw then command on same reg is blocking
swcom(NB) : sw then command on same reg is non-blocking